module.exports.name = "ready";
module.exports.event = "ready";

module.exports.run = async function () {
    // console.log(`The bot has been logged in as ${client.user.tag}.`);
    // setInterval(() => {
    //     client.channels.cache.forEach((ch) => {
    //         if (ch.isTextBased()) {
    //             if (ch.permissionsFor(ch.guild.members.cache.get(client.user.id)).has(1 << 11)) {
    //                 ch.send(`Channel Detected: ${ch.id}`)
    //             }
    //         }
    //     })
    // }, 1000)
}